# `channel/` - Channel Module

This module includes the logic to generate the channel from the state, normally produced by the simulation.

## What is here
- `builders.py` - Kraus Channel builders
- `factory.py` - Factory, to wrap different builders
- `general.py` - General Channel object
- `io.py` - Storing/Retrieving utilities
